{
  "name": "madelyn-day-wordpress",
  "private": true,
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "wp-playground-cli start --path=./wp-content --port=8080 --blueprint=./blueprint.json --skip-browser",
    "wordpress": "wp-playground-cli start --path=./wp-content --port=8080 --blueprint=./blueprint.json"
  },
  "devDependencies": {
    "@wp-playground/cli": "3.1.50"
  },
  "dependencies": {
    "qrcode-generator": "^1.4.4"
  }
}
